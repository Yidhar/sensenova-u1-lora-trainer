# Pinned to what runs on the dev box (RTX 5090, CUDA 12.8). Versions are
# observed-baselines, not strict floors — bump as upstream/drivers move.

# core
torch>=2.9
# Upper bound: transformers 5.8 reshuffled rope params API + tied_weights
# ABI in incompatible ways with the upstream `modeling_qwen3.py` we pin.
# See loader.py for the indirect-import self-heal that survives upstream
# transformers churn beyond what version-pinning catches.
transformers>=5.6,<5.8
safetensors>=0.7

# HF model load (NEOChatModel uses trust_remote_code)
huggingface_hub>=0.26
sentencepiece
einops
pillow
numpy

# low-VRAM training
bitsandbytes>=0.45    # 8-bit / 4-bit base + paged AdamW8bit
peft>=0.13            # not strictly required (we have our own LoRA), pulled in for utility helpers

# config
pyyaml
pydantic>=2.0

# tests
pytest>=7
